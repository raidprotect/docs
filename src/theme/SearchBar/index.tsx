/* Pas de barre de recherche dans la navbar : le thème classique l'ajoute
 * d'office à droite dès qu'un plugin fournit ce composant. La recherche reste
 * dans le footer, qui garde aussi le raccourci ⌘K. */
export default function SearchBar(): null {
  return null;
}
